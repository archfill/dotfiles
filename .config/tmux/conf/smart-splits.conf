# =============================================
# Smart Splits Integration (Neovim/Tmux統合)
# =============================================
# mrjones2014/smart-splits.nvim との連携設定

# Neovimプロセスチェック用変数（smart-splits公式推奨）
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

# ===== ナビゲーション (Ctrl+h/j/k/l) =====
# Neovim normalモード時のみ: Neovimに送る（insertモードではSKK/補完を優先）
# Neovim以外: tmuxでペイン切り替え
# 注意: Neovim insertモードでは、SKK有効化(<C-j>)、補完選択(<C-j><C-k>)、
#       SKK無効化・補完確定(<C-l>)として機能します
bind-key -n C-h if-shell "$is_vim" 'send-keys C-h' 'select-pane -L'
bind-key -n C-j if-shell "$is_vim" 'send-keys C-j' 'select-pane -D'
bind-key -n C-k if-shell "$is_vim" 'send-keys C-k' 'select-pane -U'
bind-key -n C-l if-shell "$is_vim" 'send-keys C-l' 'select-pane -R'

# コピーモード中のナビゲーション
bind-key -T copy-mode-vi C-h select-pane -L
bind-key -T copy-mode-vi C-j select-pane -D
bind-key -T copy-mode-vi C-k select-pane -U
bind-key -T copy-mode-vi C-l select-pane -R

# ===== リサイズ (Alt+h/j/k/l) =====
# Neovim内ならNeovimに送り、そうでなければtmuxでリサイズ
bind-key -n M-h if-shell "$is_vim" 'send-keys M-h' 'resize-pane -L 3'
bind-key -n M-j if-shell "$is_vim" 'send-keys M-j' 'resize-pane -D 3'
bind-key -n M-k if-shell "$is_vim" 'send-keys M-k' 'resize-pane -U 3'
bind-key -n M-l if-shell "$is_vim" 'send-keys M-l' 'resize-pane -R 3'

# =============================================
# 既存のキーバインドとの競合解消
# =============================================
# C-l (clear画面) の代替: Prefix + C-l
bind C-l send-keys 'C-l'
