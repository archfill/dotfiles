# Replace this file with hardware configuration generated on the target machine:
#
#   sudo nixos-generate-config --show-hardware-config > nix/hosts/<host>/hardware.nix
#
# Do not copy hardware.nix from another host.
{ ... }:

{
}
